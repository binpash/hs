#!/bin/bash

# # Input file name
INPUT="1000000"
out="$(mktemp)"

# Repeat the operation by calling compute_and_append.sh.
# Adjust the number of repetitions based on your system performance to reach the desired execution time.

./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"
./compute.sh "$INPUT" "$out"