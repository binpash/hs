#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
repo_top="${PASH_SPEC_TOP:-$(git -C "$script_dir" rev-parse --show-toplevel --show-superproject-working-tree | awk 'NF { print; exit }')}"

"$repo_top/report/setup_base" "$script_dir"
