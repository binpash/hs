# Python dependencies for pash-spec preprocessor and scheduler

# Core AST and parsing libraries
shasta~=0.5
# Floating, with a floor. libdash 0.4.0 (commit f58fffc) broke command
# line-spans -- linno_after overshot by one line whenever the parser had
# buffered ahead, so each command's text also contained the next one and hs
# executed every command but the last twice. 0.4.0, 0.4.1 and 0.5.0 are all
# affected; 0.5.1 (#38) fixes it by dropping the nleft heuristic for the
# parsefile eof flag. The floor keeps those three out while letting future
# releases in.
libdash>=0.5.1
libbash~=0.1.14

# Shell variable expansion (used by scheduler's static analysis)
sh-expand~=0.2.0

psutil
