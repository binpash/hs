# Python dependencies for pash-spec preprocessor and scheduler

# Core AST and parsing libraries
shasta~=0.5
# Pinned: libdash 0.4.0 (commit f58fffc) broke command line-spans --
# linno_after overshot by one line whenever the parser had buffered ahead, so
# each command's text also contained the next one and hs executed every command
# but the last twice. Fixed upstream in 0.5.1 (#38), which drops the nleft
# heuristic for the parsefile eof flag. Pinned rather than floating so a
# rebuild can never silently pick up a parser regression again.
libdash==0.5.1
libbash~=0.1.14

# Shell variable expansion (used by scheduler's static analysis)
sh-expand~=0.2.0

psutil
