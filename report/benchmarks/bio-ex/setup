#!/bin/bash

echo please ensure you build the Dockerfile at the root of the repo with the tag hs
for experiment in ex1 ex2 ex2-accelerated; do
    sudo docker build -t hs/$experiment ./benchmarks/bio-ex/$experiment/
done
